#pragma once

#define RTVersion 1


// If in assignment 1
#if (RTVersion==1)
	#define Hit Hit_v1
#endif // (RTVersion==0)

// If in assignment 2
#if (RTVersion==2)
#define Hit Hit_v2
#endif // (RTVersion==0)
